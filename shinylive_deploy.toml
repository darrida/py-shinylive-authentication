
[general]
app_name = "test-app"

[development]
directory = "src_dev"

[deploy.gitbranch]
prod = "main"
beta = "main"

[deploy.staging]
directory = "staging"

[deploy.local]
staging_only = true
directory = "src_test_webserver/shinyapps/"
base_url = "http://localhost:8000/apps"

[deploy.server]
host = "127.0.0.1"
user = "shinylive"
port = 2222
directory = "shinyapps"
base_url = "http://localhost:5000"
